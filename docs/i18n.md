# Sistema de Traducciones (i18n)

Este proyecto incluye un sistema de traducciones completo que permite soportar múltiples idiomas sin duplicar código.

## Estructura

```
src/
├── lib/
│   └── i18n/
│       ├── index.ts          # Configuración principal
│       ├── types.ts          # Tipos TypeScript
│       ├── es.ts             # Traducciones en español (BASE)
│       ├── en.ts             # Traducciones en inglés
│       └── fr.ts             # Traducciones en francés
├── hooks/
│   └── useI18n.tsx           # Hook y contexto de traducciones
└── components/
    └── LanguageSelector.tsx  # Selector de idioma
```

## 🔴 PROCEDIMIENTO OBLIGATORIO PARA TRADUCCIONES

### ⚠️ REGLA PRINCIPAL: SIEMPRE USAR ESPAÑOL COMO BASE

**IMPORTANTE**: Todas las traducciones DEBEN tomar como referencia exacta el archivo `es.ts`. 
Nunca traducir desde otro idioma o improvisar contenido.

### Proceso paso a paso:

#### 1. **Preparación**
- Abrir el archivo `src/lib/i18n/es.ts` como referencia
- Identificar la estructura exacta y todo el contenido español
- NO MODIFICAR el archivo español, solo usarlo como guía

#### 2. **Traducción palabra por palabra**
- Respetar la estructura exacta del español
- Mantener la misma cantidad de elementos en arrays
- Conservar el mismo nivel de detalle en descripciones
- Traducir ingredientes manteniendo especificaciones técnicas exactas
- No omitir ni agregar información

#### 3. **Elementos que NO se traducen**
- Nombres de productos específicos (ej: "Fruco")
- Datos técnicos como porcentajes (ej: "87%", "15%")
- Información de contacto (emails, teléfonos, direcciones)
- Códigos o referencias técnicas

#### 4. **Elementos que SÍ se traducen**
- Todos los textos descriptivos
- Títulos y subtítulos
- Ingredientes (manteniendo especificaciones)
- Mensajes de interfaz
- Contenido de historia y misión

#### 5. **Verificación**
- Comparar estructura final con español
- Verificar que no falte ningún campo
- Asegurar coherencia en la terminología
- Revisar que los arrays tengan la misma cantidad de elementos

### Ejemplo de traducción correcta:

**Español (BASE):**
```typescript
frito: {
  name: "Tomate frito",
  description: "Un sabor único y auténtico, hecho con ingredientes frescos y de alta calidad",
  ingredients: [
    "Tomate 87% (a partir de tomate concentrado)",
    "Aceite de girasol",
    "Azúcar"
  ]
}
```

**Francés (CORRECTO):**
```typescript
frito: {
  name: "Tomate frite",
  description: "Une saveur unique et authentique, faite avec des ingrédients frais et de haute qualité",
  ingredients: [
    "Tomate 87% (à partir de concentré de tomate)",
    "Huile de tournesol",
    "Sucre"
  ]
}
```

## Uso

### 1. Usar traducciones en un componente

```tsx
import { useTranslations } from "@/hooks/useI18n";

function MiComponente() {
  const t = useTranslations();

  return (
    <div>
      <h1>{t.hero.subtitle}</h1>
      <p>{t.common.tagline}</p>
    </div>
  );
}
```

### 2. Cambiar idioma

```tsx
import { useI18n } from "@/hooks/useI18n";

function MiComponente() {
  const { language, setLanguage } = useI18n();

  return (
    <button onClick={() => setLanguage(language === "es" ? "en" : "es")}>
      Cambiar idioma
    </button>
  );
}
```

### 3. Agregar un nuevo idioma

#### Proceso completo:

1. **Crear archivo de traducciones**
   ```bash
   # Ejemplo para italiano
   touch src/lib/i18n/it.ts
   ```

2. **Actualizar tipos TypeScript**
   ```typescript
   // En src/lib/i18n/types.ts
   export type Language = "es" | "en" | "fr" | "it";
   ```

3. **Crear traducciones tomando SOLO español como base**
   ```typescript
   // En src/lib/i18n/it.ts
   import type { Translations } from "./types";

   export const it: Translations = {
     // Traducir TODO el contenido de es.ts
     // Mantener estructura exacta
     // No omitir ningún campo
   };
   ```

4. **Registrar en configuración principal**
   ```typescript
   // En src/lib/i18n/index.ts
   import { it } from "./it";

   const translations: Record<Language, Translations> = {
     es,
     en,
     fr,
     it, // Agregar aquí
   };
   ```

5. **Crear bandera SVG**
   ```bash
   # Crear archivo en public/flags/it.svg
   # Dimensiones: 24x16px
   # Formato SVG optimizado
   ```

6. **Actualizar selector de idiomas**
   ```typescript
   // En src/components/LanguageSelector.tsx
   const languages = [
     { code: "es", name: "Español", flagSrc: "/flags/es.svg" },
     { code: "en", name: "English", flagSrc: "/flags/us.svg" },
     { code: "fr", name: "Français", flagSrc: "/flags/fr.svg" },
     { code: "it", name: "Italiano", flagSrc: "/flags/it.svg" }, // Agregar
   ];
   ```

### 4. Agregar nuevas traducciones

1. **Actualizar interfaz TypeScript**
   ```typescript
   // En src/lib/i18n/types.ts
   export interface Translations {
     // Agregar nuevos campos aquí
     newSection: {
       newField: string;
     };
   }
   ```

2. **Agregar en TODOS los archivos de idioma**
   - Empezar por `es.ts` (idioma base)
   - Luego traducir a `en.ts`, `fr.ts`, etc.
   - Mantener estructura idéntica

## Idiomas soportados

- 🇪🇸 **Español (es)** - Idioma base y por defecto
- 🇺🇸 **English (en)** - Traducción desde español
- 🇫🇷 **Français (fr)** - Traducción desde español

## Características

- ✅ Tipado completo con TypeScript
- ✅ Contexto React para manejo global del estado
- ✅ Hook personalizado para fácil acceso
- ✅ Selector de idioma con UI atractiva y banderas SVG
- ✅ Escalable para múltiples idiomas
- ✅ Sin duplicación de código
- ✅ Traduciones fieles al contenido original español
- ✅ Proceso documentado y estandarizado

## ❗ Notas importantes

- **NUNCA** traducir desde inglés u otro idioma que no sea español
- **SIEMPRE** verificar que la estructura sea idéntica al español
- **MANTENER** especificaciones técnicas exactas
- **RESPETAR** la cantidad y orden de elementos en arrays
- **CONSERVAR** el nivel de detalle del contenido original
